# Discord Go Bot Pattern

### setup:
` go mod tidy `
` go run main.go `

To enter bot settings, create an .env file, there is an example ` .env ` file named **.env-example** in the project.
```env
TOKEN = 
```
